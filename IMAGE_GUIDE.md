# 即梦影像工作台

双击 **Start-Film.cmd**（原来的 **Start-Images.cmd** 也可用），服务启动后打开 **http://127.0.0.1:7861**。

## 从文字到短视频

1. 在“文字生图”输入画面描述，选择比例。每次生成一张 5.0 模型的 2K 图片。
2. 在作品库选择满意的图片，点击“用这张图生成视频”。原有生成的图片也可以直接使用。
3. 描述人物动作与镜头运动，选择视频模型、5 秒或 10 秒，确认后提交。
4. 页面自动查询真实任务状态。生成完成后直接预览、拖动播放进度、下载原始视频。

默认使用 Seedance 2.0 Fast 会员模型、720p 验证短镜头，也可选 Seedance 2.0 会员模型或 1.0 Fast。模型权限、实际速度和额度消耗以即梦返回为准，不能保证免排队。需要账号首次合规授权时，请在即梦官方页面完成授权后核对任务记录。

2026-09-23 实测：CLI 帮助虽然列出 `seedance1.5pro`，服务端却拒绝该版本。工作台已移除该选项，避免再次提交不支持的模型。

## 额度与安全

- 使用项目内已经登录的即梦账号，每次点击生成可能消耗该账号积分；不会购买会员、积分或新的服务。
- 页面显示查询到的账号积分和平台返回的任务消耗。不把未知价格显示为免费，也不承诺固定速度。
- 同一时间最多保留一个进行中的图片任务和一个视频任务。重复点击或重复发送同一提交编号不会重复提交；网络超时造成结果不明时，不自动重试生成。
- 网页可关闭；后台程序和电脑需要继续运行，才能查询和下载。重新启动后继续查询已有远端任务，不重新生成。
- 提交结果不明且没有远端任务编号时，需要核对即梦任务记录。失败任务不会自动重新生成。
- 仅监听本机 7861 端口，不向浏览器提供即梦登录凭据；不要通过端口映射公开此本地工具。

## 文件与边界

图片和视频均保存在 `media-tasks/images/<本地任务编号>/`，其中 `job.json` 保存平台编号、状态和用量。保留原目录是为了兼容已有图片。支持断网后恢复查询、下载重试与视频分段读取。

有本地 FFmpeg 时，工作台会为完成的视频生成 `preview.webm` 兼容预览副本，使用 CPU、最多两条编码线程，不调用云端、不花积分。网页优先播放预览副本，下载按钮始终保留原始 MP4。转换失败不会丢失原片；可下载后用系统播放器打开。

本机验证样片：诸葛亮与鲁肃船上交谈，Seedance 2.0 Fast 会员模型，720×1280 竖屏，约 5 秒；2026-09-23 从提交到 MP4 下载完成约 3 分 33 秒，平台返回 30 积分。这只是一次实测，不保证每次相同速度、用量或人物一致性。

当前交付是 **文字 → 单张图片 → 5/10 秒图生视频 → 预览下载**。它还不是长章节一键生成整部短剧：自动拆分长文本、多个镜头的角色一致性、配音字幕、跨镜头剪辑仍需后续串联。不要把单个短镜头当作 90 秒完整剧集。

电脑不用运行 ComfyUI 或本地大模型。原有 7860 页面、分镜功能及 Cursor 修改的代码保留，没有被替换。

代码入口：`app/image_server.py`、`app/images.html`；生成与查询使用 Python 标准库和项目内 `.tools/dreamina.exe`，兼容预览另外使用本机 FFmpeg。

离线回归测试不会连接生成平台或消耗积分：

```powershell
.\.local\venv\Scripts\python.exe -m unittest discover -s app -p test_image_server.py
node app/test_images_ui.cjs
```

前端测试使用 DOM 模拟，不能替代真实浏览器的布局和播放测试。

## 新电脑准备

推荐 Windows + Python 3.12。云端工作台后端只使用 Python 标准库，不需要安装本地模型；Node.js 24 只用于前端离线测试，不是运行页面的必需项。

1. 安装 Python 并下载本仓库，在仓库根目录创建独立环境：

   ```powershell
   py -3.12 -m venv .local/venv
   ```

2. 按[即梦官方 CLI 安装说明](https://jimeng.jianying.com/cli)获取 Windows 工具，放置为 `.tools/dreamina.exe`。不要将登录凭据复制进公开仓库，也不要把来路不明的安装脚本直接执行。
3. 在本机终端登录自己的账号，并按官方页面完成授权：

   ```powershell
   .\.tools\dreamina.exe login
   ```

4. 可选：将 Windows FFmpeg 放置为 `.tools/ffmpeg.exe`。工作台也能识别项目虚拟环境中 `imageio_ffmpeg` 自带的可执行文件；不是任意系统 PATH 下的 FFmpeg 都会被当前代码发现。
5. 双击 `Start-Film.cmd`。如果提示环境或 CLI 缺失，先补齐对应文件；启动失败看 `.local/images-error.log`。没有可用模型权限时，不要反复重新生成来试错。

官方 CLI 的安装、登录与模型权限可能变化，以当前官方提示为准。已有部署无需重复创建环境或重新登录。离线开发无需做 CLI 登录；见 [贡献指南](CONTRIBUTING.md)。
